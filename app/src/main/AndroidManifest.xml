<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stockzilla"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".feature.HealthScoreDetailsActivity"
            android:exported="false"
            android:label="@string/health_score_details_title"
            android:parentActivityName=".feature.MainActivity" />

        <activity
            android:name=".feature.FullAnalysisActivity"
            android:exported="false"
            android:label="@string/full_analysis_title"
            android:parentActivityName=".feature.MainActivity" />

        <activity
            android:name=".analyst.EidosAnalystActivity"
            android:exported="false"
            android:label="@string/eidos_analyst"
            android:parentActivityName=".feature.FullAnalysisActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name=".feature.IndustryStocksActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".feature.MainActivity" />

        <activity
            android:name=".feature.AddPeerActivity"
            android:exported="false"
            android:label="@string/add_peer_title"
            android:parentActivityName=".feature.IndustryStocksActivity" />

        <activity
            android:name=".feature.DiagnosticLogActivity"
            android:exported="false"
            android:label="@string/diagnostic_log_title"
            android:parentActivityName=".feature.MainActivity" />

        <activity
            android:name=".ai.AiMemoryCacheActivity"
            android:exported="false"
            android:label="@string/ai_memory_menu_title"
            android:parentActivityName=".ai.AiAssistantActivity" />

        <activity
            android:name=".ai.AiAssistantActivity"
            android:exported="false"
            android:label="@string/ai_assistant_title"
            android:parentActivityName=".feature.MainActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name=".feature.PortfolioChartActivity"
            android:exported="false"
            android:label="@string/portfolio_chart_title"
            android:parentActivityName=".feature.MainActivity" />

        <activity
            android:name=".feature.GovNewsDetailActivity"
            android:exported="false"
            android:label="@string/gov_news_detail_title"
            android:parentActivityName=".feature.MainActivity" />

        <activity
            android:name=".feature.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Stockzilla">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>